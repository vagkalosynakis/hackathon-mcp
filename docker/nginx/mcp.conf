location = /mcp {
    fastcgi_pass php;
    include fastcgi_params;
    fastcgi_param SCRIPT_FILENAME /app/mcp-http.php;
    fastcgi_param REQUEST_URI     /mcp;
    fastcgi_read_timeout 600;

    # Required for SSE streaming: disable nginx buffering
    fastcgi_buffering off;
}
